[project]
name = "finance-tools"
version = "0.1.0"
description = "Temporal-powered interest rate monitoring tools"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "alembic>=1.16,<2",
    "beautifulsoup4>=4.13,<5",
    "psycopg[binary]>=3.2,<4",
    "requests>=2.32,<3",
    "sqlalchemy>=2.0,<3",
    "temporalio>=1.15,<2",
]

[project.scripts]
interest-rate-worker = "interest_rates_scraper.worker:main"

[dependency-groups]
dev = [
    "pytest>=8.4,<9",
    "pytest-cov>=6.2,<7",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["interest_rates_scraper"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
